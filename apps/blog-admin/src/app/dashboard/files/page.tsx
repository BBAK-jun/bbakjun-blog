/**
 * Files Management Page
 *
 * Renders the complete file management interface using FileManagerWidget
 */

"use client";

import { FileManagerWidget } from "@/widgets/file-manager";

export default function FilesPage() {
  return <FileManagerWidget />;
}
